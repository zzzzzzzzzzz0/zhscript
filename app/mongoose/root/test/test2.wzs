显示
“HTTP/1.1 200 OK”←↙
“Content-Type: text/html; charset=utf-8”←↙←↙
。

显示
<table空格border>
先迭代解释下代码
	别名名以参数1。
	<tr><td>‘名’</td><td>先头文字‘名’了</td></tr>↙。
上代码、
Accept、Accept-Encoding、Accept-Language、Cache-Control、Connection、Host、Referer、User-Agent了
</table><table空格border>
先迭代解释下代码
	别名名以参数1。
	<tr><td>‘名’</td><td>先配置项‘名’了</td></tr>↙。
上代码、
document_root、listening_ports、index_files、cgi_pattern了
</table>。
